name: 🎮 Galaga Contributions

on:
  schedule:
    # Roda toda domingo à meia-noite (UTC)
    - cron: "0 0 * * 0"
  workflow_dispatch: # Permite rodar manualmente pela UI do GitHub
  push:
    branches:
      - main

jobs:
  generate-galaga:
    name: Generate Galaga SVG
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 🛎️ Checkout repo
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: pip install requests

      - name: 🎮 Generate Galaga SVG
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: philippluca123
        run: python galaga_generator.py

      - name: 💾 Commit & Push SVG
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Galaga Bot 🤖"
          git add galaga-contributions.gif
          git diff --staged --quiet || git commit -m "🎮 Update Galaga contributions [skip ci]"
          git push
